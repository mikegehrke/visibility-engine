'use client';

import RoadmapPlaceholder from '@/components/shared/RoadmapPlaceholder';

export default function ImageModulePage() {
  return (
    <RoadmapPlaceholder 
      titleKey="imageModule"
      description="Advanced image generation and optimization for maximum engagement."
      features={[
        "AI image generation from prompts",
        "Automatic image optimization for web",
        "Brand-consistent visual creation",
        "Multi-platform image resizing"
      ]}
    />
  );
}
